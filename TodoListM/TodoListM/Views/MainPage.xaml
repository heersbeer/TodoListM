<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TodoListM.MainPage">

    <StackLayout Orientation="Vertical" BackgroundColor="White" Margin="10,10,10,10">
        <ListView ItemsSource="{Binding TodoItemList}" ItemTapped="OnListViewItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" VerticalOptions="Center" Margin="15,0,0,0">
                                <Label Text="{Binding Name}" Font="Bold, 20" />
                                <CheckBox IsChecked="{Binding Done}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="Add Item" BackgroundColor="Silver" FontAttributes="Bold" TextColor="Black" Clicked="AddItem"/>
    </StackLayout>

</ContentPage>
